# Vai-ter-copa

#include <stdio.h>
  
int main() {
  
    int y;

     while(scanf("%d", &y) != EOF)
    
        if (y == 0){
            printf("vai ter copa!\n");
        }

         else {
            printf("vai ter duas!\n");
        }
   
  
    return 0;
}
